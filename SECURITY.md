# Security policy

Report vulnerabilities privately through GitHub Security Advisories for
`absolutejs/secure-transfer-adapters`. Do not include live credentials, plaintext,
or decryption capabilities in an issue.

The create-only guarantee is security-critical. Backends that cannot provide an
atomic conditional write are not supported.
