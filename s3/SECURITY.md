# Security policy

Report vulnerabilities privately through GitHub Security Advisories for
`absolutejs/secure-transfer-adapters`. Use a private bucket and credentials scoped
to the configured prefix. A bucket policy that requires conditional writes is
recommended as defense in depth.
